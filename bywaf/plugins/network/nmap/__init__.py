"""Nmap support package for bundled network plugins.

This package is intentionally not a commandlet provider. It holds the optional
nmap backend adapters and shared failure diagnostics used by `hostscanner`,
`portscanner`, REPL dispatch, and network plugin tests.
"""
