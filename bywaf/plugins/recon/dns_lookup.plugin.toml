[plugin]
library_backed = true
process_wrapped = false
service = false

[[commandlets]]
name = "dns_lookup"
capabilities = [
  "db.write:dns.record",
  "db.write:dns.error",
  "network.connect",
]
module = "bywaf.plugins.recon.dns_lookup"
