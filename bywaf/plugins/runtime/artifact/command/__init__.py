"""Artifact command parsing, dispatch, and completion support.

Used by: `bywaf.plugins.runtime.artifact.ArtifactCommand` and `SearchCommand`
to keep action parsing and tab-completion helpers separate from storage and
display operations.
"""
