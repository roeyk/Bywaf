"""Artifact storage implementation package.

Used by: the public `bywaf.artifacts` module to expose artifact records, the
artifact body store, and paired artifact database helpers to command contexts,
runtime commands, reports, and tests.
"""

