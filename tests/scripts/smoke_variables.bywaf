# User-view regression script: variables, commandlet execution, and listings.
var hostscanner.targets=127.0.0.1
hostscanner $targets name=script variable expansion note=script regression smoke
runs --all
