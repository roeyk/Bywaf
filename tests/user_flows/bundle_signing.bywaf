# EXPECT: created bundle name=client-a
# EXPECT: added bundle name=client-a kind=audit
# EXPECT: added bundle name=client-a kind=evidence
# EXPECT: ok bundle name=client-a
# EXPECT: exported bundle name=client-a
# EXPECT-EVENT: bundle.created
# EXPECT-EVENT: bundle.item.added
# EXPECT-EVENT: bundle.sealed
# EXPECT-EVENT: bundle.exported

bundle create name=client-a
artifact attach run=1 file={fixture} name=user-flow-bundle note=smoke
bundle add name=client-a audit topic=artifact.attached
bundle add name=client-a evidence commandlet=artifact
bundle seal name=client-a
bundle verify name=client-a
bundle export name=client-a file={tmp}/client-a.bundle.json
